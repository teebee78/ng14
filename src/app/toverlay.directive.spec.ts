import { ToverlayDirective } from './toverlay.directive';

describe('ToverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new ToverlayDirective();
    expect(directive).toBeTruthy();
  });
});
